{
	"name": "lunaria",
	"description": "A decentralized asset manager",
	"repository": "https://github.com/darkflorist/lunaria",
	"license": "Unlicense",
	"devDependencies": {
		"typescript": "4.9.3"
	},
	"dependencies": {
		"@preact/signals": "1.1.1",
		"ethers": "6.6.4",
		"funtypes": "5.0.3",
		"preact": "10.8.1"
	},
	"scripts": {
		"build": "rm -rf app/js && tsc",
		"serve": "npx serve -L ./app",
		"vendor": "npm install && npm --prefix ./build install && npm --prefix ./build run vendor",
		"styles": "npm --prefix ./twcss install && npm --prefix ./twcss run styles"
	}
}
